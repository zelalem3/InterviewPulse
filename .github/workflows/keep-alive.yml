name: Keep Server Alive

on:
  schedule:
    - cron: '*/14 * * * *' # Runs every 14 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send HTTP request to Render backend
        run: |
          curl -I https://interviewpulse.onrender.com/health